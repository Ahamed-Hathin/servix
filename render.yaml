services:
  # Backend Web Service
  - type: web
    name: servix-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5000
      - key: CLIENT_URL
        value: https://servix-client.onrender.com 
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: GEMINI_API_KEY
        sync: false

  # Frontend Static Site
  - type: static
    name: servix-client
    runtime: static
    rootDir: client
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://servix-api.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
